# macOS metadata
.DS_Store

# Local-only private materials (transcripts, acceptance letters, certificates,
# CV sources, etc.). NEVER commit these — do not `git add -f materials/`.
# The public CV intentionally lives at assets/CV_Zongmin.pdf instead.
materials/
